<EditForm Model="FormModel" OnValidSubmit="Submit">
    <DataAnnotationsValidator />
    <ValidationSummary />
    <MatH5>Provide the Github repo path to retrieve any public file.</MatH5>
    <p>NOTE: Don't include the '/' between url sections. We'll handle that. However, if the file path for the target file is multiple folders deep, it will required a '/' between the files. The full api Url will build Below</p>
    <p>github user or organization:</p>
    <div class="mat-layout-grid">
        <div class="mat-layout-grid-inner">
            <div class="mat-layout-grid-cell mat-layout-grid-cell-span-6">
                <p>Github User or Organization</p>
                <MatTextField TValue="string" @bind-Value="FormModel.GithubName" Label="github user or organization"></MatTextField>
                <p>Repository name:</p>
                <MatTextField TValue="string" @bind-Value="FormModel.RepoName" Label="Repo name"></MatTextField>
            </div>
            <div class="mat-layout-grid-cell mat-layout-grid-cell-span-6">
                <p>Filepath (Not including FileName)</p>
                <MatTextField TValue="string" @bind-Value="FormModel.FilePath" Label="Path" PlaceHolder="Folder/SubFolder" HelperTextPersistent="true" HelperText="Required unless file is in the root of the repository (this is uncommon)"></MatTextField>
                <p>File name:</p>
                <MatTextField TValue="string" @bind-Value="FormModel.FileName" Label="file name" PlaceHolder="FileName.cs"></MatTextField>
            </div>
        </div>
    </div>
    <br/> <hr /><br />
    <MatH5>GitHub Api Url:</MatH5>
    <div style="margin-bottom:1.5em">https://api.github.com/repos/@FormModel.GithubName/@FormModel.RepoName/contents/@FormModel.FilePath/@FormModel.FileName</div>
    <MatButton Type="submit" Raised="true">Pull in code file contents</MatButton>
</EditForm>