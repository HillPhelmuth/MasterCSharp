@using MasterCSharp.Shared.CodeModels
<div>
    @foreach (var codeSamplePair in CodeSnippets.RazorSnippets)
    {
        <MatButton OnClick="@(() => UpdateActiveFile(codeSamplePair))">@codeSamplePair.Key</MatButton>
    }
</div>
