@using BlazorApp.Shared.RazorCompileService
<MatH3>Available Files</MatH3>

@foreach (var file in CodeEditorService.CodeFiles ?? new List<CodeFile>())
{
    <MatButton OnClick="@(() => UpdateActiveFile(file))">@file.Path</MatButton>
}
